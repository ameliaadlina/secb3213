{
  "$jsonSchema": {
    "bsonType": "object",
    "title": "Adverse Events",
    "description": "A single adverse event reported for a patient during a trial. Most relationship-dense entity: patient_id, trial_id, and intervention_id are all mandatory. Arm is NOT stored directly here — to find it, look up intervention_id -> interventions.arm_label. Grading follows CTCAE v5.0.",
    "required": [
      "ae_id",
      "patient_id",
      "trial_id",
      "intervention_id",
      "event_name",
      "system_organ_class",
      "ctcae_grade",
      "onset_date",
      "outcome",
      "serious",
      "action_taken",
      "causality",
      "reported_by",
      "created_at"
    ],
    "properties": {
      "ae_id": {
        "bsonType": "string",
        "pattern": "^AE-[0-9]{8}$",
        "description": "Unique. Format AE-XXXXXXXX."
      },
      "patient_id": {
        "bsonType": "string",
        "pattern": "^PT-[0-9]{6}$",
        "description": "Reference to patients.patient_id. Mandatory — a record missing this is incomplete."
      },
      "trial_id": {
        "bsonType": "string",
        "pattern": "^NCT-[0-9]{8}$",
        "description": "Reference to clinical_trials.trial_id. Mandatory."
      },
      "intervention_id": {
        "bsonType": "string",
        "pattern": "^INT-[0-9]{6}$",
        "description": "Reference to interventions.intervention_id. Mandatory. Also the indirect path to the arm: interventions.arm_label."
      },
      "event_name": {
        "bsonType": "string",
        "description": "MedDRA-style term, e.g. Neutropenia, Nausea, Elevated ALT, Rash maculopapular."
      },
      "system_organ_class": {
        "bsonType": "string",
        "description": "MedDRA SOC, e.g. Blood and lymphatic system disorders, Gastrointestinal disorders, Nervous system disorders, Hepatobiliary disorders, Skin and subcutaneous tissue disorders."
      },
      "ctcae_grade": {
        "bsonType": "int",
        "minimum": 1,
        "maximum": 5,
        "description": "CTCAE grade: 1 (Mild) - 5 (Fatal)."
      },
      "onset_date": {
        "bsonType": "date",
        "description": "Must be after the patient's enrolment date (enforced at application/ingestion level)."
      },
      "resolution_date": {
        "bsonType": ["date", "null"],
        "description": "Null if ongoing."
      },
      "outcome": {
        "bsonType": "string",
        "enum": ["Resolved", "Resolving", "Not resolved", "Fatal", "Unknown"]
      },
      "serious": {
        "bsonType": "bool",
        "description": "true if ctcae_grade >= 3 or required hospitalisation."
      },
      "action_taken": {
        "bsonType": "string",
        "enum": ["None", "Dose reduced", "Dose interrupted", "Drug withdrawn", "Supportive care", "Other"]
      },
      "causality": {
        "bsonType": "string",
        "enum": ["Unrelated", "Unlikely", "Possible", "Probable", "Definite"]
      },
      "lab_values": {
        "bsonType": ["object", "null"],
        "description": "Null if no lab data.",
        "properties": {
          "test_name": { "bsonType": "string" },
          "value": { "bsonType": "double" },
          "unit": { "bsonType": "string" },
          "reference_range": { "bsonType": "string" }
        }
      },
      "reported_by": {
        "bsonType": "string",
        "enum": ["Investigator", "Patient", "Nurse", "Sponsor"]
      },
      "created_at": { "bsonType": "date" }
    }
  }
}
